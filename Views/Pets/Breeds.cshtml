@model IEnumerable<HealthVet.Models.BreedsModel>

<p>
    <a asp-action="Create">Create New</a>
</p>

@Html.DropDownList("id", ViewBag.BreedsList as SelectList, "Seleccione la raza", new {@class = "form-control"})

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.animal_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.animal_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.animal_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.animal_name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
